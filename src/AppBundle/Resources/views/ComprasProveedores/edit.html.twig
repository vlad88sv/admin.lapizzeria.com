{% extends '::base.html.twig' %}

{% block page_sub_header %}Compras: editar proveedor{% endblock %}

{% block body -%}

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="btn btn-sm btn-info" href="{{ path('compras_proveedores') }}">
            Regresar a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
