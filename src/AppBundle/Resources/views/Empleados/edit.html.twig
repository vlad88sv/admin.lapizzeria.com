{% extends '::base.html.twig' %}

{% block page_sub_header %}Edición de empleado{% endblock %}

{% block body -%}

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="btn btn-default"  href="{{ path('empleados') }}">
            Regresar a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
