login_route:
    path:     /login
    defaults: { _controller: AppBundle:Security:login }
login_check:
    path: /login_check
    # no controller is bound to this route as it's handled by the Security system
logout:
    path:   /logout
    
app:
    resource: "@AppBundle/Resources/config/routing.yml"
    prefix:   /
